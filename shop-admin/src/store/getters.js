const getters = {
    // visitedViews: state => state.tagsView.visitedViews,
    // cachedViews: state => state.tagsView.cachedViews,
    // token: state => state.user.token,
    // avatar: state => state.user.avatar,
    // name: state => state.user.name,
    // uid: state => state.user.uid,
    // refreshFlag: state => state.menus.refreshFlag,
    // addRouters: state => state.menus.addRouters,
    // routers: state => state.menus.routers,
    // breadcrumb: state => state.menus.breadcrumb,
    // permission: state => state.menus.permission,
    // cityShow: state => state.menus.cityShow,
    // city: state => state.menus.cityChoose,
}
export default getters